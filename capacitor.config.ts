import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DuckEat_Front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
